# adapters/

Reserved for API-side wiring of `packages/source-adapters` (Greenhouse,
Lever, Ashby, Workable, generic company adapters) — implemented in
**Phase 7**, once job discovery/normalization (Phase 3) and the
application engine (Phase 6) exist to consume them.
