apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jobpilot
resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - redis.yaml
  - api-deployment.yaml
  - worker-deployment.yaml
  - web-deployment.yaml
  - ingress.yaml
# Apply secret.yaml.example as secret.yaml locally; never commit it.
